definitions:
  Label:
    description: "A model representing recurrent task labels"
    type: "object"
    properties:
      id:
        type: "string"
      name:
        type: "string"
      color:
        type: "string"
        format: "hex"
